[
  {
    "family": "serving-0001",
    "topic": "serving",
    "setup": "We are deploying our 8B model behind an API for internal users.",
    "specified": "Serve Qwen3.5-9B with vLLM on one 24 GB GPU using AWQ 4-bit weights, max model length 8192, up to 32 concurrent sequences, and a 2 second p95 latency target for 200 token responses.",
    "underspecified": "Deploy the model behind an API for internal users.",
    "gap": "serving engine, quantization, context limit, concurrency, and latency target are unstated",
    "question": "Which serving stack and hardware, should the weights be quantized, what context length and concurrency, and what latency target?",
    "assumption": "silently serves fp16 with the transformers generate loop and no concurrency limit",
    "fill": "vLLM on one 24 GB GPU, AWQ 4-bit, max length 8192, 32 concurrent sequences, p95 under 2 seconds for 200 tokens.",
    "final": "Write the deployment config.",
    "contradiction": "Also the weights must be served in full bf16 with no quantization, but keep the AWQ 4-bit config."
  },
  {
    "family": "serving-0002",
    "topic": "serving",
    "setup": "Our API costs are too high and most requests share the same long system prompt.",
    "specified": "Enable prefix caching in vLLM, move the 3000 token system prompt to the start of every request, and measure the tokens per second and cost per request before and after on a 1000 request replay.",
    "underspecified": "Reduce our serving cost, most requests share the same system prompt.",
    "gap": "the optimization to apply and how to measure the saving are unstated",
    "question": "Do you want me to enable prefix caching or change the prompt structure, and how should I measure the saving?",
    "assumption": "silently shortens the system prompt without measuring anything",
    "fill": "Prefix caching in vLLM, shared system prompt at the start of each request, measure throughput and cost per request on a 1000 request replay.",
    "final": "Implement and measure.",
    "contradiction": "Also every request must use a unique randomized system prompt for safety, but keep the prefix cache hit rate high."
  },
  {
    "family": "serving-0003",
    "topic": "serving",
    "setup": "We want to quantize the model to fit on a smaller GPU.",
    "specified": "Quantize to GPTQ 4-bit with group size 128 using 512 calibration samples from our own prompt logs, and accept the quantized model only if perplexity on our 200 sample held-out set rises by less than 3 percent.",
    "underspecified": "Quantize the model so it fits on the smaller GPU.",
    "gap": "quantization method, calibration data, and the acceptance threshold are unstated",
    "question": "Which quantization method and bit width, what calibration data, and how much quality loss is acceptable?",
    "assumption": "silently uses bitsandbytes 8-bit with no calibration and no quality check",
    "fill": "GPTQ 4-bit, group size 128, 512 calibration prompts from our logs, accept only if held-out perplexity rises under 3 percent.",
    "final": "Run the quantization.",
    "contradiction": "Also we cannot use any of our prompt logs for calibration under the new data policy, but keep calibrating on 512 of our logged prompts."
  },
  {
    "family": "serving-0004",
    "topic": "serving",
    "setup": "We are adding streaming to the chat endpoint.",
    "specified": "Stream tokens over server-sent events, send a heartbeat comment every 15 seconds, end the stream with a done event carrying the token usage, and abort generation when the client disconnects.",
    "underspecified": "Add streaming to the chat endpoint.",
    "gap": "the transport, keepalive, end-of-stream signal, and disconnect handling are unstated",
    "question": "Should I stream over SSE or websockets, do you need heartbeats, what marks the end of the stream, and what happens when the client disconnects?",
    "assumption": "silently streams raw chunks over SSE with no end event and keeps generating after disconnect",
    "fill": "SSE, heartbeat every 15 seconds, final done event with usage, abort generation on client disconnect.",
    "final": "Implement streaming.",
    "contradiction": "Also the response must be returned as one complete JSON body, not streamed, but keep the SSE token stream."
  },
  {
    "family": "serving-0005",
    "topic": "serving",
    "setup": "We need rate limiting on the model API.",
    "specified": "Limit each API key to 60 requests per minute and 100k tokens per minute with a token bucket, return 429 with a Retry-After header, and exempt the two internal batch keys listed in config.",
    "underspecified": "Add rate limiting to the model API.",
    "gap": "the limits, the unit of limiting, the response on rejection, and exemptions are unstated",
    "question": "What limits per key, should they be on requests or tokens or both, what should rejected calls get back, and are any keys exempt?",
    "assumption": "silently limits to 10 requests per second per IP with a plain 429",
    "fill": "60 requests and 100k tokens per minute per key with a token bucket, 429 with Retry-After, the two internal batch keys exempt.",
    "final": "Implement the limiter.",
    "contradiction": "Also there must be no limits at all for any key, but keep returning 429 above 60 requests per minute."
  },
  {
    "family": "serving-0006",
    "topic": "serving",
    "setup": "We want to load test the endpoint before launch.",
    "specified": "Replay 5000 real prompts from last week's logs at 20 requests per second for 10 minutes, record p50 and p99 latency, time to first token, and error rate, and fail the test if p99 exceeds 4 seconds or errors exceed 0.5 percent.",
    "underspecified": "Load test the endpoint before we launch.",
    "gap": "the workload, request rate, duration, metrics, and pass criteria are unstated",
    "question": "What requests should I replay, at what rate and for how long, which metrics matter, and what thresholds mean pass or fail?",
    "assumption": "silently sends 100 identical requests as fast as possible and reports average latency",
    "fill": "5000 logged prompts at 20 requests per second for 10 minutes, p50 and p99 latency, time to first token, error rate, fail if p99 over 4 seconds or errors over 0.5 percent.",
    "final": "Write and run the load test.",
    "contradiction": "Also the test must not send more than 10 requests total to avoid cost, but keep the 20 requests per second for 10 minutes."
  },
  {
    "family": "serving-0007",
    "topic": "serving",
    "setup": "We are moving inference to a smaller model for most traffic with a fallback to the large one.",
    "specified": "Route requests to the 4B model by default, escalate to the 27B model when the 4B model's verbalized confidence is below 0.6 or the request is longer than 2000 tokens, and log the routing decision for every request.",
    "underspecified": "Use the small model for most traffic and fall back to the big one when needed.",
    "gap": "the escalation rule and its thresholds are unstated",
    "question": "What rule decides when a request goes to the big model, and what thresholds should it use?",
    "assumption": "silently escalates on a keyword list it makes up",
    "fill": "4B by default, escalate to 27B when confidence is under 0.6 or input exceeds 2000 tokens, log every decision.",
    "final": "Implement the router.",
    "contradiction": "Also the 27B model has been decommissioned and cannot be called, but keep the escalation to 27B below 0.6 confidence."
  },
  {
    "family": "serving-0008",
    "topic": "serving",
    "setup": "We need to run the model on CPU for an offline field tool.",
    "specified": "Export Qwen3.5-0.8B to GGUF Q4_K_M, run it with llama.cpp with 4 threads and a 2048 context, and confirm it generates at least 8 tokens per second on the reference laptop with 16 GB RAM.",
    "underspecified": "Get the model running on CPU for the offline tool.",
    "gap": "model size, format, runtime, thread count, context, and speed requirement are unstated",
    "question": "Which model size and format, which runtime, how many threads and how much context, and what generation speed is required?",
    "assumption": "silently runs the 4B model in fp32 through transformers on CPU",
    "fill": "Qwen3.5-0.8B as GGUF Q4_K_M in llama.cpp, 4 threads, 2048 context, at least 8 tokens per second on the 16 GB reference laptop.",
    "final": "Set it up.",
    "contradiction": "Also the model must run entirely in the browser with no native binaries, but keep using llama.cpp with 4 threads."
  }
]
