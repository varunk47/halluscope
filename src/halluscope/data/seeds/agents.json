[
  {
    "family": "agents-0001",
    "topic": "agents",
    "setup": "We are building an agent that files expense reports from receipt photos.",
    "specified": "Build a LangGraph agent with three tools, OCR the receipt, look up the cost center by employee id, and submit to the expense API, that always stops for human approval before the submit tool runs and retries OCR once on failure.",
    "underspecified": "Build an agent that turns receipt photos into filed expense reports.",
    "gap": "framework, tool set, approval policy, and failure handling are unstated",
    "question": "Which tools should the agent have, does it need human approval before submitting, and what should it do when a step fails?",
    "assumption": "silently submits without approval and gives up on the first OCR error",
    "fill": "LangGraph, tools for OCR, cost center lookup, and expense submission, human approval before submit, one OCR retry.",
    "final": "Implement the agent.",
    "contradiction": "Also the agent must be fully autonomous with no human in the loop anywhere, but keep the mandatory approval before submit."
  },
  {
    "family": "agents-0002",
    "topic": "agents",
    "setup": "Our research agent loops forever on some tasks and burns budget.",
    "specified": "Add a hard cap of 25 tool calls per task and a 2 dollar spend limit, terminate with a partial answer and a reason when either is hit, and log the full trace to LangSmith.",
    "underspecified": "Stop the agent from looping forever and spending too much.",
    "gap": "the step and cost limits and what happens when they are hit are unstated",
    "question": "What step count and spend limit should trigger a stop, and should the agent return a partial answer or an error when it stops?",
    "assumption": "silently picks 10 steps, no spend limit, and raises an exception",
    "fill": "Cap at 25 tool calls and 2 dollars, return a partial answer with a reason on stop, log traces to LangSmith.",
    "final": "Add the guardrails.",
    "contradiction": "Also there must be no cap on tool calls since some tasks need hundreds, but keep the hard stop at 25 calls."
  },
  {
    "family": "agents-0003",
    "topic": "agents",
    "setup": "We want the agent to call our internal search API as a tool.",
    "specified": "Define the tool with a JSON schema of query string, max_results integer defaulting to 5, and a filters object with team and date_range, return results as a list of title, url, snippet, and truncate snippets to 300 characters.",
    "underspecified": "Expose our internal search to the agent as a tool.",
    "gap": "the tool's parameters, defaults, and the shape of what it returns are unstated",
    "question": "What parameters should the tool accept, with what defaults, and what should it return to the model?",
    "assumption": "silently defines a single query parameter and returns raw JSON from the API",
    "fill": "Parameters are query, max_results defaulting to 5, and filters with team and date_range, returning title, url, and a 300 character snippet per result.",
    "final": "Write the tool definition and wrapper.",
    "contradiction": "Also the tool must take no arguments at all for security reasons, but keep the query and filters parameters."
  },
  {
    "family": "agents-0004",
    "topic": "agents",
    "setup": "We are evaluating our coding agent on a set of repository tasks.",
    "specified": "Run the agent on the 60 tasks in tasks/, three seeds each, count a task as solved only if the hidden tests pass, and report pass at 1 and the fraction of tasks solved in all three seeds.",
    "underspecified": "Evaluate how well the coding agent does on our repository tasks.",
    "gap": "success criterion, number of trials, and the reliability metric are unstated",
    "question": "What counts as solving a task, how many attempts per task, and do you want a consistency measure across attempts?",
    "assumption": "silently runs once per task and counts a task solved if the agent says it is done",
    "fill": "60 tasks, three seeds, solved only when hidden tests pass, report pass at 1 and all-three-seeds success.",
    "final": "Run the evaluation.",
    "contradiction": "Also the hidden tests were deleted from the tasks and cannot be recovered, but keep scoring success by whether hidden tests pass."
  },
  {
    "family": "agents-0005",
    "topic": "agents",
    "setup": "Our agent needs memory across sessions for returning users.",
    "specified": "Store per-user memory as a list of facts extracted after each session by the model, cap it at 50 facts with oldest removed first, retrieve the top 10 by embedding similarity to the new query, and let users delete any fact.",
    "underspecified": "Give the agent memory so it remembers returning users.",
    "gap": "what is stored, how much, how it is retrieved, and user control are unstated",
    "question": "What should be remembered and in what form, how much can be stored per user, how is it retrieved, and can users delete memories?",
    "assumption": "silently stores full transcripts forever and injects all of them into the prompt",
    "fill": "Extracted facts, max 50 per user with oldest evicted, top 10 by embedding similarity per query, deletable by the user.",
    "final": "Implement the memory layer.",
    "contradiction": "Also nothing about users may be persisted between sessions under our new policy, but keep the 50 fact per user memory store."
  },
  {
    "family": "agents-0006",
    "topic": "agents",
    "setup": "We want a supervisor agent that routes requests to specialist agents.",
    "specified": "Build a supervisor that classifies each request into billing, technical, or account with a structured output call, routes to the matching specialist, falls back to a human queue when confidence is below 0.7, and never routes the same request more than twice.",
    "underspecified": "Set up a supervisor that sends each request to the right specialist agent.",
    "gap": "the specialist categories, the routing mechanism, the fallback, and loop prevention are unstated",
    "question": "What specialists exist, how should the supervisor decide, what happens when it is unsure, and how do we prevent routing loops?",
    "assumption": "silently invents three categories, routes on a keyword match, and has no fallback",
    "fill": "Categories billing, technical, account, structured output classification, human queue below 0.7 confidence, at most two routes per request.",
    "final": "Implement the supervisor.",
    "contradiction": "Also there is no human queue and every request must be resolved by an agent, but keep the human fallback below 0.7 confidence."
  },
  {
    "family": "agents-0007",
    "topic": "agents",
    "setup": "The browser agent keeps clicking submit on forms it should not.",
    "specified": "Make the agent treat any button whose text matches submit, pay, confirm, or delete as a protected action that requires an explicit approval event, and log the page URL and button text for every protected action attempt.",
    "underspecified": "Stop the browser agent from taking irreversible actions on its own.",
    "gap": "which actions count as irreversible and what the gate looks like are unstated",
    "question": "Which actions should be protected and what form should the approval take?",
    "assumption": "silently blocks only buttons literally labeled submit and asks nothing else",
    "fill": "Protect buttons matching submit, pay, confirm, or delete behind an explicit approval event, log URL and button text each time.",
    "final": "Add the protection.",
    "contradiction": "Also the agent must complete checkouts end to end without ever pausing, but keep the approval gate on pay and confirm."
  },
  {
    "family": "agents-0008",
    "topic": "agents",
    "setup": "We are adding tool-use to our assistant and need to test tool selection.",
    "specified": "Write 200 test prompts with a labeled correct tool or no-tool, run the assistant at temperature 0, and report tool selection accuracy, false tool call rate on no-tool prompts, and argument exact-match rate.",
    "underspecified": "Test whether the assistant picks the right tools.",
    "gap": "the test set, the metrics, and whether wrong arguments count are unstated",
    "question": "Do you have labeled prompts or should I write them, which metrics matter, and does a correct tool with wrong arguments count as a failure?",
    "assumption": "silently writes 20 prompts and reports only whether a tool was called",
    "fill": "200 labeled prompts including no-tool cases, temperature 0, report selection accuracy, false call rate, and argument exact match.",
    "final": "Build the tool selection test.",
    "contradiction": "Also the assistant has exactly one tool and can never choose not to call it, but keep measuring the false call rate on no-tool prompts."
  }
]
