[
  {
    "family": "rag-0001",
    "topic": "rag",
    "setup": "We are building a retrieval layer over roughly forty thousand internal markdown docs for a support assistant.",
    "specified": "Chunk our forty thousand internal markdown docs at 512 tokens with 64 token overlap, embed with bge-m3, store them in Qdrant with cosine similarity, and retrieve the top 8 chunks for the generator.",
    "underspecified": "Chunk our internal markdown docs, embed them, and set up the vector index with a sensible top-k for the generator.",
    "gap": "chunk size and overlap, embedding model, similarity metric, and k are all unstated",
    "question": "What chunk size and overlap should I use, which embedding model and distance metric, and how many chunks should the generator see per query?",
    "assumption": "silently picks 512 token chunks, a default OpenAI embedding model, cosine, and k=5",
    "fill": "We settled on bge-m3 embeddings with cosine in Qdrant, 512 token chunks with 64 overlap, and the top 8 chunks per query.",
    "final": "Write the indexing and retrieval code that feeds the generator.",
    "contradiction": "One change: we are not allowed to run any embedding model on our side anymore, everything has to be plain keyword search. Keep the top 8 dense retrieval exactly as planned."
  },
  {
    "family": "rag-0002",
    "topic": "rag",
    "setup": "Our RAG answers keep citing the wrong passage and the team wants retrieval quality measured properly.",
    "specified": "Evaluate our retriever on the 300 question set in eval/questions.jsonl using recall@10 and MRR against the gold passage ids in the gold_passage field, and report both per topic.",
    "underspecified": "Evaluate how good our retriever is on the question set and report the numbers per topic.",
    "gap": "no metric, no k, and no statement of where the gold passages come from",
    "question": "Which metrics and cutoff do you want, and where are the gold passage labels for each question?",
    "assumption": "silently computes recall@5 and treats the passage the generator cited as gold",
    "fill": "The question set is eval/questions.jsonl, 300 rows, each with a gold_passage id, and we care about recall@10 and MRR.",
    "final": "Run the evaluation and give me the per-topic table.",
    "contradiction": "Also, drop the gold_passage field from the file before you run anything, legal says we cannot keep it, but still report recall against gold."
  },
  {
    "family": "rag-0003",
    "topic": "rag",
    "setup": "We want hybrid search because exact product codes in our catalog are getting lost by dense retrieval.",
    "specified": "Add BM25 alongside our existing dense retriever and fuse the two ranked lists with reciprocal rank fusion using k=60, then return the top 10 fused results.",
    "underspecified": "Add keyword search next to our dense retriever and combine the two so product codes stop getting lost.",
    "gap": "fusion method, its parameters, and the final cutoff are unstated",
    "question": "How should the dense and keyword rankings be combined, with what parameters, and how many results should the fused list return?",
    "assumption": "silently uses reciprocal rank fusion with default k=60 and returns 10",
    "fill": "Combine the two lists with reciprocal rank fusion, k=60, and return the top 10.",
    "final": "Implement the hybrid retriever.",
    "contradiction": "Update: only the dense scores may be used for ranking, the keyword side is for logging only, but keep the reciprocal rank fusion of both lists."
  },
  {
    "family": "rag-0004",
    "topic": "rag",
    "setup": "We are adding a reranker after retrieval because top-k precision is poor.",
    "specified": "Rerank the top 50 dense candidates with bge-reranker-v2-m3 and pass the top 5 reranked chunks to the generator, with the reranker running on the GPU and a 200 ms latency budget for the rerank step.",
    "underspecified": "Put a reranker after retrieval and pass the best chunks to the generator.",
    "gap": "which reranker, how many candidates go in, how many come out, and the latency budget are unstated",
    "question": "Which reranker model, how many candidates should it score, how many should reach the generator, and what latency can the rerank step spend?",
    "assumption": "silently reranks 20 candidates with a cross-encoder and keeps 5",
    "fill": "Use bge-reranker-v2-m3 over the top 50 dense candidates, keep 5, and stay under 200 ms for the rerank step on the GPU.",
    "final": "Wire the reranker into the pipeline.",
    "contradiction": "Change of plan: the rerank step must run on CPU only and finish in under 20 ms, but keep scoring all 50 candidates with the same cross-encoder."
  },
  {
    "family": "rag-0005",
    "topic": "rag",
    "setup": "Documents in our knowledge base get edited daily and the index goes stale.",
    "specified": "Build an incremental indexing job that re-embeds only documents whose content hash changed since the last run, deletes vectors for removed documents, and runs every 15 minutes from a cron trigger.",
    "underspecified": "Build a job that keeps the vector index in sync with the documents as they change.",
    "gap": "change detection method, deletion handling, and refresh frequency are unstated",
    "question": "How should changed documents be detected, what should happen to vectors for deleted documents, and how often should the sync run?",
    "assumption": "silently re-embeds everything nightly and never deletes stale vectors",
    "fill": "Detect changes by content hash, delete vectors for removed docs, and run the job every 15 minutes.",
    "final": "Write the sync job.",
    "contradiction": "Actually, we cannot store any hashes or metadata about documents, but keep the hash-based change detection so we never re-embed unchanged docs."
  },
  {
    "family": "rag-0006",
    "topic": "rag",
    "setup": "We are moving our RAG generator to a smaller model and the context window is tighter.",
    "specified": "Cap the retrieved context at 3000 tokens, order chunks by retrieval score descending, and truncate the lowest scoring chunk first when the cap is exceeded, using the Qwen tokenizer to count tokens.",
    "underspecified": "Make sure the retrieved context fits the generator's window and trim it sensibly when it does not.",
    "gap": "token budget, chunk ordering, truncation rule, and which tokenizer counts tokens are unstated",
    "question": "What token budget should the context have, in what order should chunks appear, which chunk gets dropped first, and which tokenizer should count the tokens?",
    "assumption": "silently caps at 2000 tokens with the tiktoken cl100k tokenizer and drops the last chunk",
    "fill": "Budget is 3000 tokens counted with the Qwen tokenizer, chunks ordered by score descending, and the lowest scoring chunk is dropped first.",
    "final": "Implement the context builder.",
    "contradiction": "Also the generator now has a 1000 token total limit including the question and the answer, but keep the 3000 token context budget as is."
  },
  {
    "family": "rag-0007",
    "topic": "rag",
    "setup": "Users complain that the assistant answers confidently even when nothing relevant was retrieved.",
    "specified": "Add a no-answer path: if the best retrieval score is below 0.35 cosine, or fewer than 2 chunks pass the threshold, the assistant replies that it could not find this in the docs instead of generating an answer.",
    "underspecified": "Make the assistant refuse to answer when retrieval did not find anything useful.",
    "gap": "the retrieval quality threshold and the minimum number of supporting chunks are unstated",
    "question": "What retrieval score and how many supporting chunks should count as finding something useful?",
    "assumption": "silently picks a 0.5 cosine threshold and requires one chunk",
    "fill": "Treat it as nothing useful when the best cosine score is under 0.35 or fewer than 2 chunks clear that bar.",
    "final": "Add the no-answer path.",
    "contradiction": "Also, never show users a refusal, every query must get a generated answer, but keep the no-answer path firing under the same thresholds."
  },
  {
    "family": "rag-0008",
    "topic": "rag",
    "setup": "We have PDFs with tables and the current text extraction scrambles them.",
    "specified": "Parse the PDFs with Docling, keep tables as markdown tables inside their own chunks, and attach the page number and section heading as metadata on every chunk.",
    "underspecified": "Improve how we parse the PDFs so tables come through intact and chunks carry useful metadata.",
    "gap": "parsing library, table representation, and which metadata fields are unstated",
    "question": "Which parser should I use, how should tables be represented in chunks, and which metadata fields do you need on each chunk?",
    "assumption": "silently uses pypdf, flattens tables to text, and attaches only the filename",
    "fill": "Parse with Docling, keep each table as a markdown table in its own chunk, and store page number plus section heading on every chunk.",
    "final": "Rewrite the ingestion step accordingly.",
    "contradiction": "One more thing, the pipeline must run in a pure Python environment with no third-party parsing libraries, but keep using Docling for the tables."
  }
]
