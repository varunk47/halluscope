# HalluScope v2

Probing an LLM's internal states to detect when a task request is underspecified or inconsistent, before the assistant silently fills the gap with its own assumption.


Full README is written at the end of the build. See `docs/superpowers/plans/2026-09-09-halluscope-v2.md` for the plan.
